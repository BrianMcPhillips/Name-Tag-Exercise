const nameButton = document.getElementById('submit-button');

nameButton.addEventListener('click', () => {
    const myInput = document.getElementById('text-input');
    const textInput = myInput.value;
    const userInput = document.getElementById('username')
    username.textContent = textInput;

})

//get input 
//get value from input
//get div
//set div content to value